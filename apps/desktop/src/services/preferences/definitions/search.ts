// Stub: Search section definition (removed, kept for compilation)
import SearchIcon from '@mui/icons-material/Search';
import type { ISectionDefinition } from './types';

export const searchSection: ISectionDefinition = {
  id: 'search',
  titleKey: 'Search',
  Icon: SearchIcon,
  items: [],
};
