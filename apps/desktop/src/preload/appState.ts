/**
 * App state synchronization (placeholder)
 * Previously synced tidgi app state with wiki state.
 * Now simplified for memeloop-desktop.
 */

export async function syncTidgiStateWhenWikiLoads(): Promise<void> {
  // No-op: wiki functionality removed in memeloop-desktop
}
